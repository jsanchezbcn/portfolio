[pytest]
pythonpath = .
testpaths = tests agents skills
markers =
    unit: unit tests
    integration: integration tests requiring external services
    manual: manual validation checks
addopts = -m "not integration"
